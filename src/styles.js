import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  grid: {
    minHeight: '100vh',
    padding: '20px',
  },
  main: {
    display: 'flex',
    justifyContent: 'center',
  },
}));
